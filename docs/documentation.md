# ouija

# documentation
kinda empty here

in order to generate mock data, use the following commands: <br>
`npx prisma generate` <br><br>
(optional): *you can also generate a different sql file by using:* `python ./apps/api/prisma/dev/data_gen.py <amount>` <br><br>
`npx prisma db seed`

in order to run tests:
`npm test`
